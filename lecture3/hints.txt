django-admin startproject lecture3 ;; creates the project
python manage.py runserver ;; runs severs
python manage.py startapp hello ;; creates hello 
python manage.py migrate ;; creates tables (e.g. session table)